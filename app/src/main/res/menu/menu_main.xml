<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context="com.github.abhijit.placefinder.ui.main.MainActivity">
    <item
        android:id="@+id/action_search"
        android:icon="@android:drawable/ic_menu_search"
        android:title="Search"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_map_view"
        android:icon="@drawable/ic_action_view_map"
        android:orderInCategory="101"
        android:title="Map View"
        android:visible="true"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_list_view"
        android:icon="@drawable/ic_action_view_list"
        android:orderInCategory="101"
        android:title="List View"
        android:visible="true"
        app:showAsAction="ifRoom"/>
</menu>
